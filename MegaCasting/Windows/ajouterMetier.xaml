<Window x:Class="MegaCasting.Windows.ajouterMetier"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ajouterMetier" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label>Libellé</Label>
            <TextBox x:Name="textBoxLibelle" Width="150"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Label>Domaine Métier</Label>
            <ComboBox x:Name="listeDomaineMetier" Width="150" ItemsSource="{Binding DomaineMetiers}" SelectedIndex="0"/>
        </StackPanel>
        <Button Grid.Row="3" x:Name="boutonAjouter" Content="Ajouter" Click="boutonAjouter_Click"/>
    </Grid>
</Window>
